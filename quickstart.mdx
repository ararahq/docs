---
title: 'Quickstart'
description: 'Do zero ao primeiro envio real de WhatsApp. cURL, Node, Python, PHP e Java lado a lado, validados contra a API real.'
keywords: ['quickstart', 'primeira mensagem', 'getting started', 'hello world', 'whatsapp']
---

Você vai mandar sua primeira mensagem WhatsApp **real**. Pra isso você precisa de três coisas: um número conectado, um template aprovado e saldo na wallet. Vamos usar o `hello_world` — o template padrão da Meta, já aprovado — pra tirar o primeiro envio do papel.

<Note>
  Pra entender **antes** os conceitos por trás (janela 24h, templates HSM, pricing): leia [Como funciona a WhatsApp Business API](/concepts/whatsapp). 10 minutos que economizam dias.
</Note>

## 1. Crie sua conta e pegue a API key

Acesse o [Dashboard](https://ararahq.com/dashboard) e crie conta com Google ou email. Em **API Keys**, gere uma chave `ara_live_*` (produção — a Arara não tem modo de teste; todo envio é real).

A chave **completa só aparece uma vez**. Copie e guarde em variável de ambiente:

```bash
export ARARA_API_KEY=ara_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

<Warning>
  Nunca commite a key. Nunca exponha no frontend. Use env var, Vercel/Netlify secrets, AWS Secrets Manager.
</Warning>

## 2. Mande a primeira mensagem

O `hello_world` não tem variáveis — é o jeito mais rápido de ver uma entrega real. Use seu próprio número no WhatsApp pra confirmar que chegou:

<CodeGroup>
```bash cURL
curl -X POST https://api.ararahq.com/v1/messages \
  -H "Authorization: Bearer $ARARA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "receiver": "whatsapp:+5511987654321",
    "templateName": "hello_world"
  }'
```

```typescript Node.js
// npm install @ararahq/sdk
import { NodeSDK } from '@ararahq/sdk';

const sdk = new NodeSDK({
  apiKey: process.env.ARARA_API_KEY!,
});

const response = await sdk.messages.send({
  receiver: 'whatsapp:+5511987654321',
  templateName: 'hello_world',
});

console.log(response.id, response.status);
```

```python Python
# pip install ararahq-sdk
import os
from arara_api_sdk import AraraClient
from arara_api_sdk.models.message import SendMessageRequest

with AraraClient(api_key=os.environ["ARARA_API_KEY"]) as client:
    response = client.messages.send(SendMessageRequest(
        receiver="whatsapp:+5511987654321",
        template_name="hello_world",
    ))

print(response.id, response.status)
```

```php PHP
// composer require ararahq/sdk
use Arara\Arara;
use Arara\Config;

$sdk = new Arara(new Config(apiKey: getenv('ARARA_API_KEY')));

$response = $sdk->messages->send(
    receiver: 'whatsapp:+5511987654321',
    templateName: 'hello_world',
);

echo "{$response->id} {$response->status}";
```

```java Java
// implementation("com.ararahq:arara-java-sdk:1.8.0")
import com.ararahq.arara.sdk.Arara;
import com.ararahq.arara.sdk.models.SendMessageRequest;

Arara arara = Arara.builder()
    .apiKey(System.getenv("ARARA_API_KEY"))
    .build();

var response = arara.getMessages().send(
    SendMessageRequest.builder()
        .receiver("whatsapp:+5511987654321")
        .templateName("hello_world")
        .build()
);

System.out.println(response.getId() + " " + response.getStatus());
```

```go Go
// go install (SDK Go em breve — usando net/http direto)
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
    "os"
)

func main() {
    body, _ := json.Marshal(map[string]any{
        "receiver":     "whatsapp:+5511987654321",
        "templateName": "hello_world",
    })
    req, _ := http.NewRequest("POST",
        "https://api.ararahq.com/v1/messages",
        bytes.NewReader(body))
    req.Header.Set("Authorization", "Bearer "+os.Getenv("ARARA_API_KEY"))
    req.Header.Set("Content-Type", "application/json")
    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    fmt.Println(resp.Status)
}
```
</CodeGroup>

Response esperada (`202 Accepted`):

```json
{
  "id": "ara_msg_k9Bf2XqP",
  "status": "PENDING",
  "sender": "whatsapp:+551144440000",
  "receiver": "whatsapp:+5511987654321"
}
```

`status` começa em `PENDING` e evolui pra `SENT`, `DELIVERED` (e `READ` quando o cliente abre).

## 3. Consultar status

```bash
curl https://api.ararahq.com/v1/messages/ara_msg_k9Bf2XqP \
  -H "Authorization: Bearer $ARARA_API_KEY"
```

```json
{
  "id": "ara_msg_k9Bf2XqP",
  "status": "DELIVERED",
  "sender": "whatsapp:+551144440000",
  "receiver": "whatsapp:+5511987654321"
}
```

Prefira receber status via [webhook](/webhooks) em vez de polling.

## E agora?

<CardGroup cols={2}>
  <Card title="Como funciona a API" icon="lightbulb" href="/concepts/whatsapp">
    Janela 24h, templates HSM, qualidade do número.
  </Card>
  <Card title="Primeiro template real" icon="file-lines" href="/concepts/templates">
    `hello_world` é nosso template demo. Cria os seus.
  </Card>
  <Card title="Receber webhooks" icon="bell" href="/webhooks">
    Status, inbound, retry com HMAC.
  </Card>
  <Card title="Receitas práticas" icon="book-open" href="/recipes">
    Carrinho abandonado, OTP, BF, suporte 24/7.
  </Card>
</CardGroup>

## Pronto pra escalar

<Steps>
  <Step title="Crie seus templates">
    `hello_world` é só demo. Crie seus templates em [Templates HSM](/concepts/templates). Aprovação Meta: 1-72h.
  </Step>
  <Step title="Configure webhook">
    Receba status (`sent`, `delivered`, `read`, `failed`) em tempo real. Ver [Webhooks](/webhooks).
  </Step>
  <Step title="Liga auto-recarga">
    Saldo de créditos zerado interrompe envios. Ver [Pricing](/concepts/pricing) e [auto-recharge](/recovery).
  </Step>
</Steps>

## Erros comuns

| Sintoma | Causa provável | Fix |
|---|---|---|
| `401` (sem `code`) | Chave inválida, revogada ou ausente | Cheque `ARARA_API_KEY` (`ara_live_...`) |
| `409 CONFLICT` | Template não existe ou não aprovado | Use `hello_world` ou crie um template |
| `422 UNPROCESSABLE_ENTITY` | Texto livre fora da janela de 24h | Use template HSM. Ver [Janela 24h](/concepts/24h-window) |
| `402 INSUFFICIENT_FUNDS` | Wallet sem saldo | Recarrega via dashboard ou [auto-recharge](/recovery) |

Lista canônica em [Erros](/erros).
