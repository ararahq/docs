---
title: 'Next.js'
description: 'Integre a Arara num projeto Next.js (App Router): Server Actions, route handlers, webhook com validação de assinatura e consulta de status de mensagem.'
---

O SDK Node/TypeScript da Arara (`@ararahq/sdk`, versão publicada 1.8.1) roda no **runtime Node** do Next.js (route handlers, Server Actions). Esta página cobre App Router (Next 15+). Pages Router funciona de forma análoga.

<Note>
  **Requisitos:** Node 18+, Next 15+. Para Edge Runtime, use `fetch` direto contra a API REST (o SDK depende do `axios`, que não roda em edge).
</Note>

## Instalação

```bash
npm install @ararahq/sdk
```

## Configuração

`.env.local`:

```ini
ARARA_API_KEY=ara_live_xxx
ARARA_WEBHOOK_SECRET=whsec_xxx
```

O `ARARA_WEBHOOK_SECRET` é o secret do webhook da sua organização (quando gerado pela Arara, começa com `whsec_`). Você encontra na configuração de webhook do dashboard (https://app.ararahq.com).

Crie um singleton do SDK (evita reconstruir em hot reload):

```typescript
// lib/arara.ts
import { NodeSDK } from '@ararahq/sdk';

declare global {
  // eslint-disable-next-line no-var
  var araraInstance: NodeSDK | undefined;
}

export const arara =
  globalThis.araraInstance ??
  new NodeSDK({
    apiKey: process.env.ARARA_API_KEY!,
  });

if (process.env.NODE_ENV !== 'production') {
  globalThis.araraInstance = arara;
}
```

## Server Action: enviar no submit do form

```typescript
// app/checkout/actions.ts
'use server';

import { arara } from '@/lib/arara';
import { revalidatePath } from 'next/cache';

export async function sendOrderConfirmation(orderId: string, phone: string, customerName: string) {
  const response = await arara.messages.send(
    {
      receiver: `whatsapp:${phone}`,
      templateName: 'confirmacao_pedido',
      templateVariables: [customerName, orderId],
    },
    { idempotencyKey: `order-confirmation-${orderId}` },
  );

  revalidatePath('/admin/orders');
  return { id: response.id, status: response.status };
}
```

A resposta é devolvida com status `202` e `status` inicial (`PENDING`, por exemplo). A entrega em si chega depois, pelo webhook.

E no client component:

```tsx
// app/checkout/page.tsx
'use client';

import { sendOrderConfirmation } from './actions';
import { useTransition } from 'react';

export default function CheckoutPage() {
  const [pending, startTransition] = useTransition();

  return (
    <form action={(formData) => {
      startTransition(async () => {
        await sendOrderConfirmation(
          formData.get('orderId') as string,
          formData.get('phone') as string,
          formData.get('name') as string,
        );
      });
    }}>
      {/* campos */}
      <button disabled={pending}>{pending ? 'Enviando...' : 'Confirmar pedido'}</button>
    </form>
  );
}
```

## Route Handler: webhook

A Arara assina cada entrega com dois headers:

- `X-Arara-Timestamp`: epoch em segundos do envio.
- `X-Arara-Signature`: `sha256=` + hex do HMAC-SHA256 de `{timestamp}.{corpo cru}` com o secret da organização.

```typescript
// app/api/webhooks/arara/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { createHmac, timingSafeEqual } from 'node:crypto';

export const runtime = 'nodejs'; // não 'edge'
export const dynamic = 'force-dynamic';

const TOLERANCE_SECONDS = 300;

function isValidSignature(rawBody: string, timestamp: string, signature: string, secret: string): boolean {
  const ts = Number(timestamp);
  if (!Number.isInteger(ts) || Math.abs(Date.now() / 1000 - ts) > TOLERANCE_SECONDS) return false;
  const expected = 'sha256=' + createHmac('sha256', secret).update(`${timestamp}.${rawBody}`).digest('hex');
  const a = Buffer.from(expected);
  const b = Buffer.from(signature);
  return a.length === b.length && timingSafeEqual(a, b);
}

export async function POST(req: NextRequest) {
  const body = await req.text();
  const timestamp = req.headers.get('x-arara-timestamp') ?? '';
  const signature = req.headers.get('x-arara-signature') ?? '';

  if (!isValidSignature(body, timestamp, signature, process.env.ARARA_WEBHOOK_SECRET!)) {
    return NextResponse.json({ error: 'Invalid signature' }, { status: 401 });
  }

  const payload = JSON.parse(body);

  switch (payload.event) {
    case 'message.status_updated':
      // payload.data.id = ara_msg_... ; payload.data.status: queued, sent, delivered, read, failed...
      break;
    case 'message.received':
      // payload.data.from, payload.data.body, payload.data.type (text | media)
      break;
    case 'conversation.needs_human':
      // o assistente de IA pediu atendimento humano: payload.data.conversationId, payload.data.reason
      break;
  }

  return new NextResponse(null, { status: 204 });
}
```

<Warning>
  O HMAC depende dos **bytes exatos** do corpo. Sempre use `req.text()` (não `req.json()`) antes de validar, senão o hash não bate.
</Warning>

Responda 2xx rápido. Se a entrega falhar, a Arara tenta de novo em 30s, 2min, 15min, 1h e 4h, então o handler precisa aguentar o mesmo evento duas vezes. Use o header `X-Arara-Delivery-Id` como chave de dedupe.

## Tipos do webhook

O SDK exporta guards de tipo para evitar `any`:

```typescript
import { WebhookUtils } from '@ararahq/sdk';

const payload: unknown = JSON.parse(body);

if (WebhookUtils.isMessageStatusEvent(payload)) {
  console.log(payload.data.status, payload.data.receiver);
}
```

<Note>
  No tipo `MessageStatusData` da 1.8.1, `messageId` está comentado como `ara_msg_...`, mas a API envia nesse campo o UUID interno. O id `ara_msg_...` (o mesmo devolvido no envio) está em `data.id`, que o tipo não declara.
</Note>

## Consultar status de uma mensagem

O SDK 1.8.1 não tem método para buscar uma mensagem. O caminho recomendado é gravar o status que chega pelo webhook `message.status_updated`. Se você precisa consultar sob demanda (tela de suporte, reconciliação), chame a API direto:

```typescript
// app/api/messages/[id]/route.ts
export const runtime = 'nodejs';

export async function GET(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;

  const res = await fetch(`https://api.ararahq.com/v1/messages/${encodeURIComponent(id)}`, {
    headers: { Authorization: `Bearer ${process.env.ARARA_API_KEY}` },
    cache: 'no-store',
    signal: AbortSignal.timeout(10_000),
  });

  if (!res.ok) {
    return Response.json({ error: 'Mensagem indisponível' }, { status: res.status === 404 ? 404 : 502 });
  }

  const msg = await res.json();
  // { id, status, mode, sender, receiver, body, cost, reason }
  return Response.json({ id: msg.id, status: msg.status, reason: msg.reason });
}
```

Aqui `status` vem em maiúsculas (`PENDING`, `SENT`, `DELIVERED`, `READ`, `FAILED`...), diferente do webhook, que usa minúsculas. Quando a mensagem falha, `reason` traz o motivo.

<Warning>
  `GET /v1/messages/{id}` só devolve mensagens enviadas pelo mesmo usuário dono da chave. Mensagem enviada por outro membro da organização responde 403. A chave também precisa da permissão `READ` (ou `ADMIN`).
</Warning>

## Produção: idempotência

Em rotas que disparam envio (Server Action, handler de webhook que responde ao cliente), passe `idempotencyKey`. O SDK envia no header `Idempotency-Key`:

```typescript
await arara.messages.send(
  { receiver: 'whatsapp:+5511999999999', templateName: 'confirmacao_pedido', templateVariables: ['João', 'ARA-123'] },
  { idempotencyKey: `order-confirmation-ARA-123` }
);
```

Derive a chave do seu evento (id do pedido), não de `randomUUID()`: assim um retry da função reaproveita a mesma chave. A mesma chave devolve a mensagem original sem novo envio. Em `POST /v1/messages` isso vale mesmo se o payload mudar (a API não compara destinatário nem variáveis), então nunca reaproveite a chave para outra mensagem.

## Próximos passos

<CardGroup cols={2}>
  <Card title="Receitas práticas" icon="book-open" href="/recipes">
    Carrinho abandonado, confirmação de pedido, suporte.
  </Card>
  <Card title="SDK Node completo" icon="node" href="/sdks/node">
    Métodos disponíveis na versão publicada.
  </Card>
</CardGroup>
